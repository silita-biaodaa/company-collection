<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.silita.biaodaa.dao.AptitudeDictionaryMapper" >

    <select id="getMajorNameBymajorUuid" parameterType="String" resultType="String">
        SELECT `majorName` FROM hunanyitihua.aptitude_dictionary WHERE majorUuid = #{majorUuid}
    </select>
</mapper>